to generate ids (it has version eg: v3,v2,v4...)
npm i --save uuid

express validator 
provide middlewares to validate incoming data.

google geocoding api
To convert address to coordinates.

encodeURIComponent
it will encode argument to url friendly format.
to get rid of special characters or white space.

mongoose unique validator (eg:emails of users)